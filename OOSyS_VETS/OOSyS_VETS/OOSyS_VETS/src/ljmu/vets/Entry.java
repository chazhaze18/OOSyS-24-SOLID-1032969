package ljmu.vets;

/**
 * Entry point for the Veterinary Surgery Management System.
 */
public class Entry {
    /**
     * Main method to start the application.
     * 
     * @param args Command-line arguments (not used).
     */
    public static void main(String[] args) {
        // Instantiate Sys and start the entry menu
        new Sys().entryMenu();
    }
}
